#!/bin/sh

for file in problem*.py;
do
    echo ""
    filename=$(basename ${file%.*})
    echo "Executing  $filename"
    if test -f $filename.in; then
        `which python` $file  < $filename.in
    else
        `which python` $file  
    fi

    echo ""
done
